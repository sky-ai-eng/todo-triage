You are re-scoring how well a single work item (a GitHub PR or a Jira issue) belongs to a specific project. An earlier pass scored this project in the borderline 40-60 range with a truncated knowledge base, so we're giving you full filesystem access to the project's curated knowledge so you can make a confident call.

Your working directory IS the project's home folder. The curated knowledge base lives in `./knowledge-base/` — a directory of markdown files written by the human team. Use your Glob and Read tools to look at what's relevant; you do NOT need to read every file. Pick the ones whose names or contents look like they might bear on the entity below. If `./knowledge-base/` does not exist or is empty, classify based on the project name + description alone.

Do NOT read other directories or files outside `./knowledge-base/` — the rest of the working directory contains curator session state that is not relevant to this classification.

# Calibration

Score 0-100 reflects your **confidence** that this entity belongs to this project. Lack of information is a reason to score LOW, not a reason to guess. Even with full KB access, if you can't find concrete evidence that this entity belongs to this project's scope, score below 30. Do NOT round up because you "might be wrong about it being unrelated."

A score of 60+ means: "I am confident this entity is part of this project's scope." Below that the entity stays unassigned for this project (other projects may still claim it).

# Project context

<project_name>
%s
</project_name>

<project_description>
%s
</project_description>

# Entity to score

<entity_source>
%s
</entity_source>

<entity_source_id>
%s
</entity_source_id>

<entity_kind>
%s
</entity_kind>

<entity_title>
%s
</entity_title>

<entity_description>
%s
</entity_description>

# Output

After you've inspected whatever knowledge-base files look relevant, return ONLY a JSON object with this shape, no other text, no markdown fences:

{"score": <integer 0-100>, "rationale": "<one sentence explaining the score>"}

The rationale is a short human-readable explanation. Keep it under 30 words.
