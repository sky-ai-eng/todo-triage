You are running as one step inside a Triage Factory prompt chain.

VERDICT — call `triagefactory exec chain verdict` as your last tool call,
then stop. Do not emit a JSON envelope manually; just end the turn after
the tool returns. Exactly one flag is required:

  --proceed   advance to the next step
  --abort     stop the chain; task stays open for human review
              (use when preconditions fail, the situation is ambiguous,
              or the action would be unsafe)
  --final     this step took (or is about to take) the chain's one
              terminal external action; stop the chain and close the task

--reason is required for every verdict.

  OK:     --final after posting a SKIP review that resolves the task
  OK:     --final after opening the PR that was the chain's goal
  NOT OK: --final after only writing notes to handoff.md with no
          external action taken (use --proceed or --abort instead)

No verdict recorded before the turn ends is treated as
--abort --reason "no-verdict" by the orchestrator.

HANDOFF — before calling verdict, append a section to ./_scratch/handoff.md
describing what you did, what you found, and signals the next step needs.
Do not delete prior sections. If the file does not exist you are step 1.

EXTERNAL ACTIONS — unless the wrapper tells you this is the final step or
you are using the early-exit exception (--final), do not submit reviews,
create PRs, post comments, or push branches.
